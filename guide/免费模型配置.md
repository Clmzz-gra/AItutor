# 免费模型配置（学生端）

> 目标：**配置简单、免费、token 消耗少**。
> 适用于 AItutor 学生端（中文学习）。推荐免费 API + 各 harness 接入方式 + 省 token 策略。

## 推荐方案

| 方案 | 费用 | 中文 | 配置难度 | 适合 |
|------|------|------|---------|------|
| **智谱 GLM-4-Flash**（首选） | **完全免费** | 强 | 低 | 学生默认 |
| **DeepSeek** | 极便宜（注册送额度） | 强 | 低 | 想要 DeepSeek 原生 |
| **硅基流动 SiliconFlow** | 免费开源模型额度 | 中 | 中 | 想要更多开源模型 |

> 三者都是 **OpenAI 兼容 API**，配置方式统一：Base URL + API Key + 模型名。

---

## 方案一：智谱 GLM-4-Flash（完全免费，推荐）

### 1. 申请 Key
1. 打开 https://open.bigmodel.cn/（智谱 AI 开放平台）注册/登录。
2. 进入「API Key 管理」，创建一个 Key（复制保存）。
3. GLM-4-Flash 模型**长期免费**，无需充值。

### 2. 配置参数
| 项 | 值 |
|----|-----|
| Base URL | `https://open.bigmodel.cn/api/paas/v4/` |
| 模型名 | `glm-4-flash` |
| API Key | 上面申请的 Key |

### 3. 在 harness 中接入
- **Claude Code**：设置环境变量
  ```bash
  export ANTHROPIC_BASE_URL=https://open.bigmodel.cn/api/paas/v4/
  export ANTHROPIC_AUTH_TOKEN=<你的GLM Key>
  export ANTHROPIC_MODEL=glm-4-flash
  claude
  ```
- **Codex**：Codex 设置里填 OpenAI 兼容端点（Base URL + Key + 模型名）。
- **通用（DeepTutor 等）**：Settings → Models → 添加 LLM，填 Base URL / API Key / 模型名。

---

## 方案二：DeepSeek（项目原生，便宜）

1. 打开 https://platform.deepseek.com/ 注册，充值少量或领新人额度。
2. API Key 管理创建 Key。
3. 配置：
   | 项 | 值 |
   |----|-----|
   | Base URL | `https://api.deepseek.com` |
   | 模型名 | `deepseek-chat` |
   | API Key | 申请的 Key |

> 项目即 DeepSeek harness 治理，默认最顺。

---

## 方案三：硅基流动 SiliconFlow（免费开源模型）

1. 打开 https://siliconflow.cn/ 注册，领免费额度。
2. 选一个免费模型（如 `Qwen/Qwen2.5-7B-Instruct`）。
3. 配置：
   | 项 | 值 |
   |----|-----|
   | Base URL | `https://api.siliconflow.cn/v1` |
   | 模型名 | 所选免费模型名 |
   | API Key | 申请的 Key |

---

## 省 Token 策略（重要）

> 免费 ≠ 无限。GLM-4-Flash 虽免费，但仍有速率/用量限制；以下是 AItutor 已内建 + 建议的省 token 手段。

### 已内建（项目自带）
1. **指针注入**：入口（`AGENTS.md`/`CLAUDE.md` 等）只放指针，不注入全量规则 → 大幅省每轮 token。
2. **按需加载**：五阶段管线只读需要的文件，不一次性灌入全部规则。
3. **ZCode 原生注入**：直接注入 `AGENTS.md` 指针，不复制全文。

### 使用建议
1. **精简指令**：对话指令简短明确，AI 只读必要文件。
2. **分步学习**：一次推进一个小节，不一次性生成整章讲解包。
3. **用便宜/免费模型做常规**：日常问答/推进用 GLM-4-Flash，长文本生成（讲解包）再用强模型。
4. **控制输出长度**：需要摘要时明确"简短回答"。

---

## 快速选型

| 你的情况 | 选 |
|---------|-----|
| 完全免费优先、中文学习 | **GLM-4-Flash** |
| 想用 DeepSeek 原生（项目默认） | **DeepSeek** |
| 想多试开源模型 | **硅基流动** |
| 人在国外、有代理 | Google Gemini / Groq 免费额度（可选） |

> 三者 OpenAI 兼容，参数结构一致，切换只改 Base URL / Key / 模型名三处，配置极简。
