# AItutor 默认免费模型配置模板 —— 智谱 GLM-4.5-Flash（免费档中能力最强）
#
# 用法：
#   1) 把本文件复制/参考为 .env（或按你的 harness 设置环境变量）
#   2) 把 YOUR_GLM_API_KEY 换成你在 https://open.bigmodel.cn 申请的 Key
#
# Claude Code 等支持 ANTHROPIC_* 环境变量的 harness：
ANTHROPIC_BASE_URL=https://open.bigmodel.cn/api/paas/v4/
ANTHROPIC_AUTH_TOKEN=YOUR_GLM_API_KEY
ANTHROPIC_MODEL=glm-4.5-flash

# 兼容旧免费档（如需）：glm-4-flash-250414 或 glm-4-flash
# 若你的 harness 走 OpenAI 兼容变量，可参考（未全部实测，以你的 harness 文档为准）：
# OPENAI_BASE_URL=https://open.bigmodel.cn/api/paas/v4/
# OPENAI_API_KEY=YOUR_GLM_API_KEY
# OPENAI_MODEL=glm-4.5-flash
